{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python 디버거: FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app.main:app", // Update the module path to app.main:app
                "--reload"
            ],
            "jinja": true
        }
    ]
}
